# Log4j2 × Amazon EFS × ECS ローリングアップデート
## ローテーション前ファイルの FD を掴み続ける問題 — 完全解説

作成日: 2026-09-18
対象読者: アプリ開発者 / SRE / インフラ担当（付録の「小学生向け」章は前提知識なしで読めます）

---

## 0. 3行でわかる結論

1. **起こります。** 新タスクがローテーション前の inode を掴んだまま生き残り、最大で1ロール周期（このケースでは1時間）ぶんのログを「回転済みファイル」の中に書き続ける状態は、構造上ふつうに発生します。
2. **しかしそれは二次的な問題です。** そもそも「2タスクが EFS 上の同一ファイルを同時に開いて追記する」時点で、NFS では `O_APPEND` が原子的でないため、**デプロイのたびに100%の確率**でログの上書き・欠損が起きています。
3. **根治策は1つだけ：同じパスを2つのプロセスに書かせないこと。** 最短の実装は「ログを stdout に出して FireLens/awslogs で送る」か、「`fileName` を書かない（DirectWrite）＋ パスにタスクIDを入れる」です。これで rename も共有も消滅し、全シナリオが同時に解決します。

---

## 1. まずは「たとえ話」から（前提知識ゼロで読めます）

### 1.1 ノートと名前シールの話

学校の**共同の倉庫**に、みんなで使う大きなノートが置いてあります。

- **ノートそのもの**（紙の束）＝ Linux でいう **inode（アイノード）**
- ノートに貼ってある**名前シール**（「たろうのノート」）＝ **ファイル名**
- 「ぼく、いまこのノートを使ってます」という**整理券** ＝ **FD（ファイルディスクリプタ）**

ここで一番大事なルールはこれです。

> **整理券は「名前シール」ではなく「ノートそのもの」に結びついている。**

だから、誰かがノートの**名前シールを貼り替えても**（＝ `rename`）、整理券を持っている人は**何も気づかず、同じノートに書き続けます**。

### 1.2 0時に起きていること

夜の12時になると、当番の子（＝旧タスク）が決まった仕事をします。

1. ノートを閉じて、整理券を返す
2. 名前シールを「たろうのノート」→「たろうのノート（9月17日23時ぶん）」に貼り替える ← **rename**
3. 新しいまっさらなノートを持ってきて、「たろうのノート」のシールを貼る ← **新規作成**

ところがこのとき、**別の子（＝新タスク）が、シールを貼り替える直前に「たろうのノート」の整理券をもらっていた**とします。

その子は何も知らないので、**「9月17日23時ぶん」というシールが貼られた古いノートに、0時以降のできごとを書き続けます**。

そして当番の子は交代して帰ってしまいます（＝旧タスクが停止）。残ったのは、**古いノートに書き続けている新しい子**だけ。

- 「たろうのノート」（新しいまっさらなノート）は **誰も書かないので空っぽ**
- 0時〜1時の記録は、なぜか **「23時ぶん」のノートの中にある**
- 誰もそれに気づかない

これがご質問の「新タスクとして残ってしまう」状態です。**答えは「はい、残ります」**。

### 1.3 もう一つの、もっと頻繁な事故

そもそも**2人が同じノートの同じページに同時に書く**と、字が重なってぐちゃぐちゃになります。

普通の机の上（＝ローカルディスク）なら、「いま何ページ目まで書いたか」を係の人（＝カーネル）が瞬時に管理してくれるので、2人でもきれいに順番に書けます。

でも**共同倉庫のノート（＝ EFS / NFS）**では、倉庫まで取りに行くのに時間がかかるので、2人とも「たしか120ページ目まで書いてあったよね」という**古い記憶**で書き始めます。結果、**121ページ目に2人が同時に書いて、片方の記録が消えます**。

これは「0時だから」ではなく、**2タスクが重なっている間ずっと**起きています。

---

## 2. 前提の整理

| 項目 | 設定値 | 備考 |
|---|---|---|
| ロガー | Apache Log4j 2 `RollingFileAppender` | |
| `fileName` | `/mnt/efs/logs/trace.log`（仮） | 固定名の「現在書き込み中」ファイル |
| `filePattern` | `.../trace-%d{yyyy-MM-dd-HH}.log`（仮） | 回転後の名前 |
| トリガポリシー | `TimeBasedTriggeringPolicy` | `interval` **未指定** |
| `interval` の既定値 | `1` | |
| 実際のロール周期 | **1時間** | `%d` パターンの**最小単位 `HH`** が周期を決める |
| `modulate` の既定値 | `false` | `interval=1` では影響なし |
| ストレージ | Amazon EFS（NFSv4.1） | 複数タスクが同一ディレクトリをマウント |
| ECS デプロイ | `minimumHealthyPercent=100` / `maximumPercent=200` | **旧タスクと新タスクが必ず同時に存在する** |
| デプロイ時刻 | 毎日 00:00 | |
| ログ書き込み | 30秒間隔で継続 | |

### 2.1 ここで一番効いてくる「隠れた前提」3つ

1. **Log4j2 のロールオーバは「タイマー」では起きない。** 次のログイベントが来たときに `isTriggeringEvent()` で判定されます。つまり **実際の回転時刻は 00:00:00 ちょうどではなく、00:00:00 + (0〜30秒)** です。書き込み間隔が長いほど、回転は遅れます。
2. **`fileName` の FD は JVM 起動時（Log4j2 の設定読み込み時）に1回開かれ、ロールオーバのときまで閉じられない。** つまり FD は何時間も生き続けます。
3. **Log4j2 には複数 JVM 間の排他機構がない。** ファイルロックを取る仕組みは `FileAppender` の `locking` 属性だけで、`RollingFileAppender` にはありません。Log4j2 のドキュメントも「複数の JVM から同一のローリングファイルへ書くことはサポートしない」という趣旨を示しています（正確な文言はバージョン差あり／要確認）。

---

## 3. Linux の FD（ファイルディスクリプタ）の本当の姿

ここが全ての事故の原理です。じっくり見ます。

### 3.1 FD は「3段構造」になっている

```
 プロセス（JVM）                カーネル                        EFS（NFSサーバ）
┌────────────────┐    ┌──────────────────────────┐   ┌──────────────────────┐
│ fd テーブル     │    │ オープンファイル記述      │   │ inode #12345         │
│  0: stdin      │    │  (open file description)  │   │  中身: ログ本文       │
│  1: stdout     │    │  ┌────────────────────┐  │   │  サイズ: 1,048,576   │
│  2: stderr     │    │  │ #7                 │  │   │  リンク数: 1          │
│  ...           │    │  │  offset = 1048576  │──┼──▶│                      │
│  9: ───────────┼───▶│  │  flags  = O_APPEND │  │   └──────────────────────┘
└────────────────┘    │  │  inode  = #12345   │  │              ▲
                      │  └────────────────────┘  │              │ 名前で参照
                      └──────────────────────────┘   ┌──────────┴───────────┐
                                                     │ ディレクトリ           │
                                                     │  "trace.log" → #12345 │
                                                     └──────────────────────┘
```

| 段 | 名前 | 誰のものか | 何を持っているか |
|---|---|---|---|
| 1 | fd テーブル | **プロセスごと** | 整数 → オープンファイル記述 へのポインタ |
| 2 | オープンファイル記述 | **`open()` の呼び出しごと** | 現在のオフセット、`O_APPEND` などのフラグ |
| 3 | inode | **ファイルシステム全体で1つ** | 実データ、サイズ、リンク数、mtime |

**決定的なポイント：段3（inode）に「ファイル名」は入っていません。** 名前はディレクトリ側が持っている、ただの「inode番号への矢印」です。

### 3.2 `rename()` したときに何が起きるか

```
rename("trace.log", "trace-2026-09-17-23.log")
```

変わるのは **ディレクトリの矢印のラベルだけ**：

```
  【rename 前】                         【rename 後】
┌──────────────────────┐          ┌──────────────────────────────┐
│ "trace.log"  → #12345│          │ "trace-2026-09-17-23.log"    │
└──────────────────────┘          │                     → #12345 │
          ▲                        └──────────────────────────────┘
          │                                       ▲
     FD #9 は #12345 を指している            FD #9 は依然 #12345 を指している
     （矢印は一切動いていない）               （何も起きていないのと同じ）
```

- **開いている FD は、名前ではなく inode に結びついている**ので、rename に**まったく影響されません**。
- FD を持っているプロセスは、**rename されたことを知る手段すらありません**（通知もエラーもない）。
- だから logrotate は rename のあと `SIGHUP` を送って「開き直せ」とアプリに教えるのです。**この「開き直し」が無い＝今回の構成**です。

これが **「FD を掴み続ける」現象の正体**です。バグでも異常でもなく、UNIX の設計どおりの正しい動作です。

### 3.3 `unlink()`（削除）したときに何が起きるか — と `.nfs` ファイル

```
unlink("trace-2026-09-17-23.log")   ← リンク数が 1 → 0 になる
```

- しかし **まだ FD を持っている人がいる間は、inode は消えません。** 「名前のない、開かれているファイル」として生き続けます。
- `ls` には出ないのに `df` では容量が減らない、という有名な現象です（`du` と `df` が食い違う）。
- 最後の FD が閉じた瞬間に、はじめて実体が消えます。

**NFS（＝EFS）ではこれが素直にできません。** NFS サーバは「誰がこのファイルを開いているか」をクライアントごとに面倒見きれない設計から出発したため、Linux の NFS クライアントは**シリーレネーム（silly rename）**という裏技を使います。

```
unlink("trace-2026-09-17-23.log")
   ↓ 自分（同じクライアント）がまだ開いている
rename("trace-2026-09-17-23.log", ".nfs00000000000ABCDEF00000001")
```

- EFS のディレクトリに **`.nfs0000....` という謎のファイル**が出てくるのはこれです。
- FD が閉じられると消えます。逆に言うと、**FD を掴んでいる間はストレージ課金され続けます**。
- そして **消えるときに中身も一緒に消えます**。そこに書き込まれていたログは**回収不能**です。

> **要検証ポイント**：削除（または rename での上書き）を行ったのが**別ホスト（別のNFSクライアント）**の場合、silly rename は効きません。その場合、FD を持っている側は次の書き込みで `ESTALE`（NFS4ERR_STALE）を受け取る可能性があります。NFSv4 はサーバ側に OPEN 状態を持つため実装依存であり、EFS での実挙動は実機検証を推奨します。

### 3.4 `O_APPEND` は NFS では原子的ではない ← 最重要

ローカルのファイルシステム（ext4/xfs）では、`O_APPEND` 付きの `write()` は**原子的**です。カーネルが inode のロックを取り、「現在のファイル末尾」を確定させてから書くので、**複数プロセスが同時に追記しても混ざりません**。ログを複数プロセスで共有できるのはこの性質のおかげです。

**NFS にはこの仕組みがありません。** NFS の `WRITE` 手続きは「オフセットを指定して書く」しかなく、「末尾に書く」という手続きが存在しないのです。そのため Linux の NFS クライアントは、

1. サーバに属性を問い合わせてファイルサイズを取得（あるいはキャッシュを使う）
2. そのサイズをオフセットとして `WRITE(offset=N, data)` を発行

という**2段階のエミュレーション**を行います。この 1 と 2 のあいだに別クライアントが書き込むと、**同じオフセットに2つのプロセスが書き込み、片方が消えます**。

`man 2 open` に、これは明記されています。

> **O_APPEND** may lead to corrupted files on NFS filesystems if more than one process appends data to a file at once. This is because NFS does not support appending to a file, so the client kernel has to simulate it, which can't be done without a race condition.

**訳：** NFS 上では、複数のプロセスが同時に1つのファイルに追記すると、ファイルが壊れる可能性がある。NFS は「追記」をサポートしていないため、クライアント側のカーネルが真似をするしかなく、それは競合状態なしには実現できないからである。

つまり、**今回の構成は、ロールオーバの話を一切抜きにしても、man page が名指しで「壊れる」と書いている使い方そのもの**です。

典型的な症状：

- 行が**まるごと消える**（上書きされた）
- 行の途中から別の行が始まる（`2026-09-18 00:00:302026-09-18 00:00:35 INFO ...`）
- ファイルの一部が **NUL バイト（`\0`）の穴**になる（一方がサイズを伸ばし、他方が古いサイズで書いたため未書き込み領域ができる）
- ファイルサイズが**減る**ことすらある

### 3.5 close-to-open 一貫性 — 「相手の書き込みは見えない」

NFS のキャッシュ一貫性モデルは **close-to-open (CTO)** です。

- 書き手が `close()` したものを、読み手が `open()` したときに、はじめて最新が見える
- 逆に言うと、**開きっぱなしのファイルについて、他のクライアントの書き込みは見えない**

Log4j2 は `fileName` の FD を**何時間も開きっぱなし**にします。したがって：

- タスクA は「自分が書いた分」しか把握していないファイルサイズを持っている
- タスクB も同様
- **両者のサイズ認識は最初からずれている** → 3.4 の追記競合が確実に起きる

さらに `bufferedIO=true`（既定）だと、Log4j2 側の 8KB バッファにも溜まるので、「どの行がいつ実際にサーバへ届いたか」はもはや誰にも分かりません。

---

## 4. Log4j2 の `TimeBasedTriggeringPolicy` が実際にやっていること

### 4.1 `interval` 未指定のとき、周期は誰が決めるのか

**`filePattern` の `%d{...}` に含まれる最小の時間単位**が周期を決めます。Log4j2 内部の `PatternProcessor` が日付パターンを走査し、以下の優先順位で「ロール頻度」を判定します。

| パターン中の文字 | 判定される頻度 |
|---|---|
| `S`（ミリ秒） | MILLISECONDLY |
| `s`（秒） | SECONDLY |
| `m`（分） | MINUTELY |
| `H` / `K` / `h` / `k`（時） | **HOURLY** ← 今回 |
| `a`（午前/午後） | HALFDAILY |
| `d` / `D` / `F` / `E`（日） | DAILY |
| `W` / `w`（週） | WEEKLY |
| `M`（月） | MONTHLY |
| `y` / `Y`（年） | ANNUALLY |

`%d{yyyy-MM-dd-HH}` には `H` があるので **HOURLY**。`interval` の既定値 `1` と掛け合わせて **1時間ごと**にロールオーバします。

> ここが誤解されやすい点です。「`interval` を設定していない＝ロールしない」ではなく、**「1時間ごとにロールする」が既定**です。0時だけでなく、**1時、2時…毎正時すべてで rename が走ります**。

`modulate` は「`interval` が 2 以上のときに境界を単位の倍数に揃えるか」のオプションなので、`interval=1` では実質無影響です。

### 4.2 ロールオーバは「時計」ではなく「次のログ出力」で起きる

```java
// 概念コード（RollingFileManager / TimeBasedTriggeringPolicy）
public boolean isTriggeringEvent(LogEvent event) {
    return event.getTimeMillis() >= nextRolloverMillis;
}
```

- **監視スレッドもタイマーもありません。**
- `append()` が呼ばれるたびに `checkRollover(event)` が走り、条件を満たしていれば回転します。
- したがって **実際の回転時刻 = 00:00:00 + (次のログ出力までの待ち時間)**。
- 今回は30秒間隔なので、**回転は 00:00:00〜00:00:30 のどこか（一様分布、平均+15秒）**になります。
- **もしそのタスクが1時間まったくログを出さなければ、ロールオーバは1回も起きません**（そして次に出したときに、何時間も前の名前で1回だけ回転します → シナリオF）。

### 4.3 ロールオーバの内部シーケンス（ここに隙間がある）

```
 ① writeFooter()                      … Layout のフッタを書く
 ② closeOutputStream()                … 現在の FD を close
 ③ 同期アクション = FileRenameAction   … rename(trace.log → trace-…-23.log)
        ┌──────────────────────────────────────────────┐
        │  ★ この瞬間、"trace.log" という名前は存在しない │  ← 隙間（後述のシナリオC）
        └──────────────────────────────────────────────┘
 ④ 非同期アクション（gz 圧縮が設定されている場合）… 別スレッドで開始
 ⑤ createFileAfterRollover()          … new FileOutputStream(fileName, append) で作り直す
```

- **③ と ⑤ は原子的なペアではありません。** ローカルなら数マイクロ秒ですが、**EFS ではメタデータ操作1回あたりネットワーク往復が発生する**ため、③〜⑤ の隙間は概ね **1〜5ミリ秒**（負荷やスループットモード次第でもっと広がる）になります。
- ③ の rename に失敗した場合の扱いはバージョン差がありますが、**いずれも「開き直して書き続ける」方向に倒れ**、失敗は `StatusLogger`（既定で stderr の ERROR レベル）にしか出ません。**アプリからは静かに壊れます。**
- `ignoreExceptions` の既定値は `true` なので、アペンダ内の例外はアプリ側に伝播しません。

### 4.4 「次の回転時刻」の初期値は `lastModified` から決まる

JVM 起動時（＝新タスクの Log4j2 初期化時）、`RollingFileManager` はおおよそ次のように初期値を決めます。

```java
long initialTime = file.exists() ? file.lastModified() : System.currentTimeMillis();
nextRolloverMillis = patternProcessor.getNextTime(initialTime, interval, modulate);
```

`getNextTime()` は **`initialTime` をパターンの精度で切り捨ててから interval を足す**ので、

| 新タスクが読み取った `lastModified` | 計算される次回回転時刻 |
|---|---|
| 23:59:52 | **00:00:00**（もう過ぎている → 最初の書き込みで即ロール） |
| 00:00:07 | 01:00:00（正常） |

**この違いが、後述するシナリオの分岐点になります。**

---

## 5. 事故シナリオ — ミリ秒単位のタイムライン

記号：`#1`, `#2`, … は inode 番号。`A` は旧タスク、`B` は新タスク。

### 5.1 シナリオ A：2タスクが同一 inode に同時追記（**毎デプロイ100%発生**）

```
時刻          タスクA（旧）              タスクB（新）            EFS 上の状態
──────────────────────────────────────────────────────────────────────────────
00:00:45                                 open("trace.log") → #2   trace.log → #2
                                         （Aと同じ inode）
00:01:00      write(#2) "…A…"
00:01:05                                 write(#2) "…B…"
                        ↑ 両者とも「自分が知っているファイルサイズ」を
                          オフセットとして WRITE を発行する（3.4 参照）
                          → 同じオフセットに書き、片方が消える
```

- **発生条件**：重複期間中、両タスクが同じファイルを開いている → **ローリングアップデートのたびに必ず**
- **症状**：行の欠損、行の途中割り込み、NUL 穴、ファイルサイズの逆行
- **重要**：これはロールオーバとは無関係に起きます。**一番頻度が高く、一番見落とされている問題です。**

### 5.2 シナリオ B：新タスクが「回転前の inode」を掴む ← **ご質問の本命**

前提：`D`（Bがファイルを open した時刻 − 00:00:00）< `Δ`（Aが実際に回転した時刻 − 00:00:00）

```
時刻          タスクA（旧）                        タスクB（新）              EFS 上の状態
─────────────────────────────────────────────────────────────────────────────────────────
23:59:52      write(#1)                                                       trace.log → #1
23:59:58                                           JVM起動 / Log4j2 初期化
23:59:59                                           open("trace.log") → #1     ★ #1 を掴んだ
                                                   lastModified=23:59:52
                                                   → nextRollover = 00:00:00
──────────────────────── 00:00:00 時境界 ────────────────────────────────────────────────
00:00:10                                           write → 回転判定 true
                                                   close(#1)
                                                   rename(trace.log → trace-…-23.log)
                                                                              trace-…-23.log → #1
                                                   open O_CREAT → #2          trace.log → #2
                                                   write(#2)
00:00:22      write → 回転判定 true
              close(#1)  ※Aが持っていた #1 の FD
              rename(trace.log → trace-…-23.log)
                 ↑ いま trace.log は #2！
                 ↑ しかも rename 先 trace-…-23.log は既に #1 が占有
                 → POSIX の rename は上書き = #1 のリンクを外す
                                                                              trace-…-23.log → #2
                                                                              #1 は名前を失う
              open O_CREAT → #3                                               trace.log → #3
              write(#3)
00:01:xx      ── SIGTERM / 停止 ──
                                                   write(#2)  ← #2 は今 "trace-…-23.log"
                                                   （Bはそれを知らない）
                       ↓↓↓ Aが消えたあと ↓↓↓
01:00:00 まで                                      Bは #2（= trace-…-23.log）に
                                                   書き続ける（丸1時間）
                                                                              trace.log(#3) は
                                                                              誰も書かず放置
```

**最終的に何が起きたか**

| ファイル | 実際に入っている内容 |
|---|---|
| `trace-2026-09-17-23.log` | 23時台のログ **＋ 0時〜1時のログ（全部）** ← 混入 |
| `trace.log` | Aが停止するまでの数十秒ぶんだけ。以後**空のまま** |
| `trace-2026-09-18-00.log`（01:00の回転で生成） | **ほぼ空** |
| 消滅した `#1` | 23時台前半のログ。silly rename（`.nfs*`）になるか ESTALE。**回収不能** |

> **ご質問への直接の回答：**
> 「回転済みファイルの FD を掴んだまま、新タスクとして生き残る」は **起こります**。
> ただし持続時間には2つのパターンがあります。
> - **数十秒で終わるパターン**：B自身も直後にロールオーバして FD を開き直す場合（B が「後にロールした」場合）
> - **丸1時間続くパターン**：**A が B より後にロールオーバした場合**。B が正常に作った `trace.log` を A が横から rename で持って行ってしまうため、B は自分の inode が改名されたことに気づけず、**次の正時（01:00）まで回転済みファイルに書き続けます**。A は直後に停止するので、**壊れた状態だけが新タスクに残ります。**
>
> つまり **「A と B のどちらが後にロールオーバしたか」が運命の分かれ目**で、その確率はおおむね **1/2** です。

### 5.3 シナリオ C：rename と create の「隙間」で open する

```
00:00:22.000   A: rename(trace.log → trace-…-23.log)      trace.log が存在しない
00:00:22.002                                B: open("trace.log", O_CREAT) → #9   ← 隙間に命中
00:00:22.003   A: open("trace.log", O_APPEND) → #9        Aも #9 を開いてしまう
               → 以後シナリオAと同じ2重書き込み
```

- **窓の幅**：EFS のメタデータ往復 ≈ 1ms × 2操作 ≒ **2ミリ秒程度**
- **発生確率**：後述のとおり **10⁻⁵ オーダー（数十年に1回）**。実務上は無視してよいレベルです。
- ただし「隙間がある」こと自体は、**設計として同一パスを共有してはいけない**根拠になります。

### 5.4 シナリオ D：rename 衝突で回転済みファイルが消える

5.2 のタイムライン中の `00:00:22` がこれです。

- POSIX の `rename()` は、**移動先がすでに存在していれば黙って上書き（元のリンクを外す）**します。エラーになりません。
- したがって **先にロールした側が作った回転済みファイルが、後からロールした側に丸ごと潰されます。**
- 潰された inode を誰かが開いていれば `.nfs*` に化け、開いていなければ即座に消滅します。
- **発生条件はシナリオBとまったく同じ**（両タスクが同じ時境界をまたいで生存）なので、**BとDはセットで起きます**。

### 5.5 シナリオ E：gz 圧縮と FD 保持の競合

`filePattern` が `trace-%d{yyyy-MM-dd-HH}.log.gz` のように圧縮指定を含む場合：

1. A が rename して、**非同期スレッド**で gz 圧縮を開始
2. 圧縮スレッドは元ファイルを**読みながら** gz を作る
3. **その間 B はまだ同じ inode に追記している**
4. 圧縮完了後、`DefaultRolloverStrategy` は元ファイルを **削除**する
5. B の FD が残っているので → **`.nfs*` に化ける** → B が閉じるまで容量を食い続ける

結果：
- **途中で切れた gz**（圧縮開始時点までの内容しか入らない）
- EFS 上に `.nfs*` が残り、**`ls` で見えるのに消せない**（`rm` しても新しい `.nfs*` になるだけ）
- EFS のストレージ課金だけ増える

### 5.6 シナリオ F：無書き込み期間をまたいだ「古い名前での回転」

タスクが数時間ログを出さなかった場合、`nextRolloverMillis` はずっと過去のまま残ります。次に書き込んだ瞬間に**1回だけ**回転が起き、回転後のファイル名は**そのとき使われるべきだった過去の時刻**になります。既に同名のファイルがあれば、シナリオDと同じく**上書き消滅**します。

今回は30秒間隔で書き続けている前提なので、平常時には起きません。ただし**デプロイ直後にログが一時的に止まるアプリ**（起動処理が長い、ヘルスチェック待ち等）では該当し得ます。

---

## 6. 「新タスクとして残ってしまう」のか — まとめた回答

| 問い | 回答 |
|---|---|
| 新タスクが回転前ファイルの FD を掴むことはあるか | **ある**（シナリオB）。旧タスクの回転より前に新タスクがファイルを open すれば確実に発生 |
| その状態が「新タスクとして残る」か | **残る**。旧タスクが停止しても、新タスクの FD は inode を指したまま。誰も是正しない |
| どのくらい続くか | **旧タスクが後にロールした場合、次の正時まで（最大1時間）**。旧タスクが先にロールした場合は、新タスク自身の次の書き込み（最大30秒）まで |
| 検知されるか | **されない**。Log4j2 は `StatusLogger` にしか出さず、`ignoreExceptions=true`（既定）でアプリにも伝わらない |
| 自然に直るか | 次の正時の回転で FD は開き直されるので**一旦は復旧する**。ただし**翌日のデプロイでまた同じことが起きる** |
| 永続的に壊れるケースはあるか | **ある**。rename が `ESTALE`/`EACCES` で失敗し続ける場合、および `.nfs*` に化けたまま容量を掴み続ける場合 |

---

## 7. 発生頻度の見積り

### 7.1 モデル

記号：

| 記号 | 意味 | 今回の値 |
|---|---|---|
| `W` | ログ書き込み間隔 | 30 秒 |
| `Δ` | 旧タスクの実際の回転時刻 − 時境界。`Δ ~ U(0, W)` | 平均 15 秒 |
| `D` | 新タスクがログファイルを open した時刻 − 時境界 | デプロイ構成に依存 |
| `G` | rename 完了 〜 create 完了 の隙間 | 約 2 ミリ秒 |

`D ~ U(a, b)` と仮定すると、

```
P_B（新タスクが回転前 inode を掴む）= P(D < Δ)

  L = max(a, 0),  U = min(b, W)

  P(D < Δ) = max(0, -a)/(b-a)
             + ( 1/((b-a)·W) ) · [ W·(U-L) − (U² − L²)/2 ]      ※ U > L のときのみ第2項
```

- **`P_A`（2タスク同時追記）= 1.0** — 重複期間がある限り必発
- **`P_D`（rename 衝突で回転済みファイルが消える）≈ `P_B`** — 発生条件が同一
- **`P_B1h`（新タスクが丸1時間、回転済みファイルに書き続ける）≈ `P_B` × 0.5** — 旧タスクが後にロールする確率が約 1/2
- **`P_C`（rename〜create の隙間に命中）= `G / (b − a)`**

### 7.2 シナリオ別の計算（毎日0時デプロイ、365回/年）

| # | シナリオ | `D` の範囲 | `P_B` | `P_B1h` | 年間 `P_B1h` 回数 | `P_C` 年間回数 |
|---|---|---|---|---|---|---|
| S1 | デプロイ起動が 00:00:00 ちょうど。Fargate で起動所要 40〜90秒 | [40, 90] 秒 | **0 %** | 0 % | 0 | 0（構造上起こり得ない） |
| S2 | EC2＋イメージキャッシュ済み、軽量JVM。起動所要 10〜40秒 | [10, 40] 秒 | **22.2 %** | 11.1 % | **約 41 回** | 0.016（約62年に1回） |
| S3 | スケジューラのジッタ ±60秒あり | [−60, +90] 秒 | **50.0 %** | 25.0 % | **約 91 回** | 0.005（約205年に1回） |
| S4 | パイプラインが 23:57 に起動し、タスクが 23:58〜23:59 に立つ | [−180, −30] 秒 | **100 %** | 50.0 % | **約 183 回** | 0（`D` が常に境界前のため） |
| S5 | 起動所要が 0〜300秒に大きくばらつく | [0, 300] 秒 | **5.0 %** | 2.5 % | **約 9 回** | 0.002（約411年に1回） |

`P_C` は「新タスクの open が、旧タスクの rename〜create の隙間 `G` に命中する」確率で、
`P_C = G × （[a,b] と [0,W] の重なり長）/ ((b−a)·W)` で算出しています。
`D` の範囲が `[0, W]` と重ならないシナリオ（S1・S4）では構造上ゼロになります。

**読み方**

- **`P_A` は全シナリオで 100%**。つまり `365 回/年` のログ破損は**既に起きている**と考えてください。これが最優先課題です。
- シナリオ B/D は、**「新タスクが立つ時刻」が 00:00:00 より前に寄るほど急激に悪化**します。S4 のように前日中にタスクが立つ構成なら**毎日100%**です。
- **シナリオ C（rename〜create の隙間）は無視してよい**レベルです。ここを心配する必要はありません。
- **注意**：S1 で `P_B = 0` でも「安全」ではありません。`P_A = 100%` は変わらないからです。

### 7.3 感度分析 — 書き込み間隔 `W` の影響

`D ~ U(−60, +90)` 固定で `W` だけ変えると：

| 書き込み間隔 `W` | `P_B` | コメント |
|---|---|---|
| 1 秒 | 40.3 % | ほぼ `P(D<0)` の 40%。境界後の窓はわずか1秒 |
| 5 秒 | 41.7 % | |
| 10 秒 | 43.3 % | |
| **30 秒（今回）** | **50.0 %** | |
| 60 秒 | 60.0 % | |
| 120 秒 | 77.5 % | |
| 300 秒 | 91.0 % | 回転が最大5分遅れる＝窓が5分開く |

> **直感に反する重要な結論：ログの書き込み間隔が長いほど危険です。**
> ロールオーバは「次のログ出力」でしか起きないので、**書き込みが疎なほど回転が遅れ、新タスクが回転前ファイルを掴む窓が広がる**からです。
> 「トレースログを減らせば安全」は**逆効果**です。

---

## 8. 対策案の全比較

| # | 案 | 効果 | 解決するシナリオ | 工数 | 副作用・注意点 | 推奨度 |
|---|---|---|---|---|---|---|
| 1 | **stdout へ出力し awslogs / FireLens(Fluent Bit) で送る** | **根治** | A B C D E F 全部 | 中 | 既存のEFS直読みバッチの改修が必要。CloudWatch Logs の取り込み課金 | ★★★★★ |
| 2 | **`fileName` を省略（DirectWrite）＋ パスにタスクIDを埋める** | **根治** | A B C D E F 全部 | **小** | ファイル数増加。収集側の対応。`Delete` で世代管理 | ★★★★★ |
| 3 | パスにタスクIDを埋める（`fileName` は従来どおり） | 根治 | A B C D E F 全部 | 小 | rename は残るが自タスク内で閉じるので安全 | ★★★★★ |
| 4 | **`fileName` を省略（DirectWrite）のみ**（パスは共有のまま） | 部分的 | B C D E（rename が消える） | 小 | **A（同時追記）が残る** ので不十分 | ★★☆☆☆ |
| 5 | ログを EFS ではなくタスクのエフェメラルストレージに書き、サイドカーで転送 | 根治 | A B C D E F 全部 | 中 | Fargate は既定 20GiB（最大200GiB）。タスク終了時に消えるので転送の確実性設計が必要 | ★★★★☆ |
| 6 | `minimumHealthyPercent=0` にして重複をなくす | 根治（ただし別コスト） | A B C D E F 全部 | 小 | **ダウンタイムが発生**。可用性要件を満たさない場合が多い | ★★☆☆☆ |
| 7 | `CronTriggeringPolicy` で回転時刻をデプロイから離す（例 `0 30 * * * ?`） | 緩和のみ | B D の確率低下 | 小 | **A は残る**。`TimeBasedTriggeringPolicy` では正時以外に回せないため、ずらすならこれ | ★★☆☆☆ |
| 8 | デプロイ時刻を 00:00 から 00:20 などへ移動 | 緩和のみ | B D の確率低下 | 極小 | **A は残る**。今日すぐできる応急処置としては有効 | ★★★☆☆（応急） |
| 9 | `immediateFlush="true"` ＋ `bufferedIO="false"` | ほぼ無効 | なし | 極小 | 行の混在は減るが **NFS の追記競合は消えない**。I/O が増えて EFS コスト増 | ★☆☆☆☆ |
| 10 | `createOnDemand="true"` | **逆効果** | なし | 極小 | rename 後にファイルを作らないので **③〜⑤ の隙間がむしろ拡大** | ☆☆☆☆☆ |
| 11 | `filePattern` に `%i` を入れて `DefaultRolloverStrategy max` を使う | 部分的 | D の一部 | 小 | 両タスクが同じ `%i` を計算する競合が残る | ★☆☆☆☆ |
| 12 | `FileAppender` の `locking="true"` を使う | 使えない | — | — | **`RollingFileAppender` には `locking` 属性がない**。かつ NFS ロックはアドバイザリ | ☆☆☆☆☆ |
| 13 | `SocketAppender` / `SyslogAppender` で集約サービスへ送る | 根治 | A B C D E F 全部 | 中〜大 | 受け側の構築・冗長化が必要。接続断時のバッファ設計 | ★★★☆☆ |
| 14 | `AsyncAppender` / `AsyncLogger` を使う | 無効 | なし | 小 | プロセス内の非同期化にすぎず、**プロセス間競合には一切効かない** | ☆☆☆☆☆ |
| 15 | EFS を Provisioned Throughput / Elastic にする | 無効 | なし | 小 | 隙間 `G` がわずかに縮むだけ。原理的解決ではない | ☆☆☆☆☆ |
| 16 | `interval="1"` を**明示的に書く** | 無効（可読性向上） | なし | 極小 | 挙動は変わらないが、「周期は `%d` の最小単位で決まる」という罠を設定ファイルに残せる | ★★★☆☆（併用） |
| 17 | ALB の `deregistration_delay` と `stopTimeout` を短縮 | 緩和のみ | 重複時間の短縮 | 小 | 処理中リクエストの打ち切りリスク | ★★☆☆☆ |
| 18 | 監視・検知の追加（第11章） | 検知のみ | なし | 小 | 対策1〜3と**必ず併用**する | ★★★★☆（併用） |

### 8.1 推奨の組み合わせ

```
【本命】 案1（stdout + FireLens）
            または
         案2（DirectWrite + タスクID別パス）
                    ＋
【併用】 案16（interval 明示）
         案18（監視）
                    ＋
【今日の応急処置】 案8（デプロイ時刻を 00:20 へ）
```

**案2 を強く推奨する理由**：ログ設定ファイル1箇所と entrypoint 数行の変更で、**シナリオ A〜F が同時に、かつ原理的に消滅する**からです。「rename が起きない（DirectWrite）」と「同じパスを2つのプロセスが開かない（タスクID別パス）」の2点を同時に満たすので、Linux の FD 挙動に依存した壊れ方が入り込む余地がなくなります。

---

## 9. 推奨構成と設定例

### 9.1 案1：stdout へ出す（もっとも推奨）

```xml
<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" name="app">
  <Appenders>
    <Console name="STDOUT" target="SYSTEM_OUT">
      <!-- JsonTemplateLayout は log4j-layout-template-json が必要 -->
      <JsonTemplateLayout eventTemplateUri="classpath:EcsLayout.json"/>
    </Console>
  </Appenders>
  <Loggers>
    <Root level="info">
      <AppenderRef ref="STDOUT"/>
    </Root>
  </Loggers>
</Configuration>
```

ECS タスク定義側（`awslogs` の例）：

```json
{
  "logConfiguration": {
    "logDriver": "awslogs",
    "options": {
      "awslogs-group": "/ecs/my-app",
      "awslogs-region": "ap-northeast-1",
      "awslogs-stream-prefix": "app"
    }
  }
}
```

- ファイルも FD もローテーションも**存在しなくなる**ので、本資料の全シナリオが消えます。
- ログストリームは**タスクごとに自動で分離**されます。
- 12-Factor App の第XI条「ログをイベントストリームとして扱う」そのものです。

### 9.2 案2：EFS を使い続ける場合の最小変更（DirectWrite ＋ タスクID別パス）

**`log4j2.xml`**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" name="app">

  <Properties>
    <!-- entrypoint.sh で export したタスク固有ID。未設定なら unknown -->
    <Property name="TASK_ID">${env:ECS_TASK_ID:-unknown}</Property>
    <Property name="LOG_DIR">/mnt/efs/logs/${TASK_ID}</Property>
  </Properties>

  <Appenders>
    <!-- ★ fileName を「書かない」ことが最重要。
         → DirectWriteRolloverStrategy が選択され、rename が一切発生しない。
         ★ パスに TASK_ID を含めることで、2タスクが同一ファイルを開かない。 -->
    <RollingFile name="TRACE"
                 filePattern="${LOG_DIR}/trace-%d{yyyy-MM-dd-HH}.log"
                 append="true"
                 immediateFlush="true"
                 bufferedIO="false">

      <PatternLayout pattern="%d{ISO8601} [%t] %-5level %logger{36} - %msg%n"/>

      <Policies>
        <!-- interval は既定値と同じ 1 だが、意図を明示するため必ず書く。
             ロール周期は filePattern の %d の最小単位（ここでは HH）で決まる。
             %d{yyyy-MM-dd}    にすると日次
             %d{yyyy-MM-dd-HH} にすると毎正時（＝現状） -->
        <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
      </Policies>

      <!-- DirectWrite では max ではなく Delete で世代管理する -->
      <DirectWriteRolloverStrategy>
        <Delete basePath="${LOG_DIR}" maxDepth="1">
          <IfFileName glob="trace-*.log"/>
          <IfLastModified age="P7D"/>
        </Delete>
      </DirectWriteRolloverStrategy>

    </RollingFile>
  </Appenders>

  <Loggers>
    <Root level="info">
      <AppenderRef ref="TRACE"/>
    </Root>
  </Loggers>
</Configuration>
```

**`entrypoint.sh`**

```sh
#!/bin/sh
set -eu

# ECS タスクメタデータエンドポイント v4 から TaskARN を取得し、末尾のタスクIDを使う。
# jq が無いイメージでも動くよう sed で抽出している。
if [ -n "${ECS_CONTAINER_METADATA_URI_V4:-}" ]; then
  TASK_ARN=$(curl -sf --max-time 3 "${ECS_CONTAINER_METADATA_URI_V4}/task" \
             | sed -n 's/.*"TaskARN"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p') || true
  if [ -n "${TASK_ARN:-}" ]; then
    ECS_TASK_ID="${TASK_ARN##*/}"
  fi
fi

# フォールバック（ローカル実行・メタデータ取得失敗時）
: "${ECS_TASK_ID:=$(hostname)-$$}"
export ECS_TASK_ID

mkdir -p "/mnt/efs/logs/${ECS_TASK_ID}"

exec java -jar /app/app.jar
```

**なぜこれで全部解決するのか**

| シナリオ | 消える理由 |
|---|---|
| A（同時追記） | パスがタスクごとに違うので、そもそも同じ inode を開かない |
| B（回転前 inode を掴む） | rename が発生しない（DirectWrite）。かつパスも共有していない |
| C（rename〜create の隙間） | rename も create-after-rename も発生しない |
| D（rename 衝突） | rename が発生しない |
| E（gz と FD の競合） | 他プロセスが同じ inode を持たない |
| F（古い名前での回転） | 回転名が他タスクと衝突しない |

**注意点**

- **ファイル数が増えます。** `Delete` アクションでの世代管理と、EFS のディレクトリあたりのエントリ数を意識してください。ディレクトリを日付で切る（`/mnt/efs/logs/2026-09-18/<task-id>/`）と運用しやすくなります。
- **収集側（EFS を直接読むバッチや Fluentd 等）の改修が必要**です。glob を `trace-*.log` → `*/trace-*.log` のように変えます。
- `${env:ECS_TASK_ID:-unknown}` のデフォルト値構文は Log4j2 のプロパティ置換でサポートされています（バージョンによる差異は要確認）。

### 9.3 案8：応急処置としてデプロイ時刻をずらす

```
デプロイ実行時刻: 00:00 → 00:20
```

- 重複期間（新タスク起動〜旧タスク停止、通常1〜10分）が**毎正時をまたがなくなる**ため、シナリオ B/D の確率が大きく下がります。
- **シナリオ A は消えません。** あくまで本対策までのつなぎです。
- ロール周期そのものを正時からずらしたい場合は `TimeBasedTriggeringPolicy` では不可能（境界は必ず時間単位の切れ目）なので、`CronTriggeringPolicy` を使います。

```xml
<Policies>
  <!-- 毎時30分に回転させる（Quartz 形式: 秒 分 時 日 月 曜日） -->
  <CronTriggeringPolicy schedule="0 30 * * * ?"/>
</Policies>
```

---

## 10. 検証・再現手順

### 10.1 現在 FD がどのファイルを掴んでいるかを見る

コンテナ内（`/proc` が見えること）：

```sh
# JVM の PID を特定
PID=$(pgrep -f 'java .*app.jar' | head -1)

# その JVM が開いているログ関連の FD を列挙
ls -l /proc/$PID/fd | grep -i 'log'
```

出力例（**異常な状態**）：

```
lrwx------ 1 root root 64 Sep 18 00:15 9 -> /mnt/efs/logs/trace-2026-09-17-23.log
```

→ **現在時刻は 00:15 なのに、23時ぶんのファイルを掴んでいる** ＝ シナリオB が発生中。

正常なら：

```
lrwx------ 1 root root 64 Sep 18 00:15 9 -> /mnt/efs/logs/trace.log
```

削除済み inode を掴んでいる場合：

```
lrwx------ 1 root root 64 Sep 18 00:15 9 -> /mnt/efs/logs/.nfs00000000000ABCDEF00000001
# または
lrwx------ 1 root root 64 Sep 18 00:15 9 -> /mnt/efs/logs/trace-…-23.log (deleted)
```

`lsof` が使えるなら：

```sh
lsof +D /mnt/efs/logs
lsof /mnt/efs/logs/trace.log
```

### 10.2 inode 番号で「名前と実体のズレ」を確認する

```sh
# ファイル名 → inode
stat -c '%i %n' /mnt/efs/logs/trace*.log

# FD → inode
readlink -f /proc/$PID/fd/9
stat -c '%i %n' "$(readlink -f /proc/$PID/fd/9)"
```

### 10.3 `.nfs*` ファイルの検出

```sh
find /mnt/efs/logs -maxdepth 2 -name '.nfs*' -printf '%p\t%s bytes\t%TY-%Tm-%Td %TH:%TM\n'
```

見つかったら、**どのプロセスが掴んでいるか**を特定します：

```sh
for p in /proc/[0-9]*; do
  ls -l $p/fd 2>/dev/null | grep -q '\.nfs' && echo "PID ${p##*/}"
done
```

### 10.4 手元で再現する最小テスト（EFS を使わずに原理だけ確認）

```sh
#!/bin/sh
# rename されても FD が古い inode を掴み続けることの確認
set -eu
cd "$(mktemp -d)"
echo "dir: $PWD"

# 書き手を起動（FD 3 に trace.log を開いて開きっぱなしにする）
( exec 3>> trace.log
  i=0
  while [ $i -lt 20 ]; do
    echo "line $i $(date +%T.%N)" >&3
    i=$((i+1))
    sleep 0.5
  done
) &
WRITER=$!

sleep 2
echo "--- rename する ---"
mv trace.log trace-OLD.log
touch trace.log          # 新しい trace.log を作る（Log4j2 の create 相当）

wait $WRITER
echo
echo "=== trace.log（新しく作ったほう）==="
cat trace.log            # ← 空。誰も書いていない
echo "=== trace-OLD.log（rename されたほう）==="
cat trace-OLD.log        # ← rename 後の行も全部ここに入っている
```

**期待される結果**：`trace.log` は空で、rename 後に出力した行まで全部 `trace-OLD.log` に入っています。これが「FD を掴み続ける」現象そのものです。

### 10.5 2タスク同時追記を EFS 上で再現する

2つのホスト（別の EC2 インスタンス）から同じ EFS をマウントし、同時に実行：

```sh
# ホスト1
for i in $(seq 1 10000); do echo "HOST1-$i" >> /mnt/efs/test/shared.log; done

# ホスト2（同時に）
for i in $(seq 1 10000); do echo "HOST2-$i" >> /mnt/efs/test/shared.log; done
```

検証：

```sh
wc -l /mnt/efs/test/shared.log          # 20000 行未満なら欠損あり
grep -c '^HOST1-' /mnt/efs/test/shared.log
grep -c '^HOST2-' /mnt/efs/test/shared.log
grep -cP 'HOST\d-\d+HOST' /mnt/efs/test/shared.log   # 行の途中割り込み
grep -c $'\0' /mnt/efs/test/shared.log  # NUL 穴
```

---

## 11. 監視と検知

対策を入れるまでの間、および入れた後の回帰検知として、以下を推奨します。

| # | 監視項目 | 実装 | 検知できるもの |
|---|---|---|---|
| 1 | FD が掴んでいるファイル名と現在時刻の整合性 | `readlink /proc/<pid>/fd/*` を定期チェックし、`trace.log` 以外なら警報 | シナリオ B（最重要） |
| 2 | `.nfs*` ファイルの存在 | `find /mnt/efs/logs -name '.nfs*'` を日次 | シナリオ D/E |
| 3 | 回転済みファイルに「その時間帯より後のタイムスタンプ」が混入していないか | 日次バッチで各回転ファイルの最終行の時刻を検査 | シナリオ B/D |
| 4 | 回転済みファイルのサイズが極端に小さい（ほぼ空） | 日次バッチで前日比較 | シナリオ B（空の `trace-…-00.log`） |
| 5 | ハートビート行の欠損 | 各タスクが30秒ごとに `HEARTBEAT <task-id> <seq>` を出力し、集計側で連番の欠けを検出 | シナリオ A（同時追記による欠損） |
| 6 | Log4j2 `StatusLogger` の ERROR | `<Configuration status="WARN">` にし、コンテナの stderr を CloudWatch Logs へ。`Unable to rename` 等をメトリクスフィルタで拾う | rename 失敗 |
| 7 | EFS の `StorageBytes` / `MeteredIOBytes` の異常増 | CloudWatch アラーム | `.nfs*` による容量リーク |

**監視1のサンプル実装**

```sh
#!/bin/sh
# check-log-fd.sh : ログFDが現行ファイルを指しているか確認
set -eu
EXPECT="/mnt/efs/logs/trace.log"
NG=0
for p in /proc/[0-9]*; do
  [ -d "$p/fd" ] || continue
  for fd in "$p"/fd/*; do
    t=$(readlink "$fd" 2>/dev/null) || continue
    case "$t" in
      */trace*.log|*/.nfs*)
        if [ "$t" != "$EXPECT" ]; then
          echo "NG pid=${p##*/} fd=${fd##*/} -> $t"
          NG=1
        fi
        ;;
    esac
  done
done
exit $NG
```

---

## 12. 歴史と背景 — なぜこの問題が生まれたのか

### 12.1 1970年代：UNIX が「名前」と「実体」を分けた

Ken Thompson と Dennis Ritchie が設計した UNIX ファイルシステムは、

- **inode**（実体・属性・データブロックへのポインタ）
- **ディレクトリエントリ**（名前 → inode番号 の対応表）

を**完全に分離**しました。これにより `link()` で1つの実体に複数の名前を付けられ、`open()` が返す FD は「名前」ではなく「実体への権限（ケーパビリティ）」になりました。

**この設計判断が、50年後の今日、EFS の上で起きている事故の直接の原因です。** 悪い設計だったわけではありません。むしろこの分離があるからこそ、「使用中のファイルを安全に差し替える」というアトミックなデプロイ手法（`rename()` による置換）が成立しています。

### 12.2 1980年代後半〜1996年：ログローテーションの誕生

ログファイルは放っておくと無限に増えます。これに対処するため、

- **`newsyslog`**（1980年代後半、BSD 系。USENET ニュースサーバのログ肥大化対策として生まれたと言われる）
- **`logrotate`**（1996年頃、Erik Troan により Red Hat Linux 向けに作られた）

が登場しました。どちらも基本動作は同じです。

```
1. mv app.log app.log.1        ← rename。FD を持つプロセスは気づかない
2. kill -HUP <pid>             ← 「開き直せ」とアプリに教える  ★これが必須
3. アプリが close() して open() し直す
```

**ステップ2があるのは、まさに 3.2 で説明した「FD は rename に気づかない」という性質があるからです。**

`logrotate` には `copytruncate` オプションもありますが、これは「アプリに HUP を送れない場合」の妥協策で、**コピーと truncate のあいだに書かれたデータは失われます**。「完全な解決策が存在しないので、どちらかの欠点を選べ」という状態が30年前から続いているわけです。

**そして今回の構成には、ステップ2にあたるものが存在しません。** Log4j2 は自分でロールオーバするので、自分の FD は開き直します。しかし **他のプロセスの FD を開き直させる手段はありません。**

### 12.3 1984年：NFS が「ステートレス」を選んだ

Sun Microsystems の NFS（1984年公開、NFSv2 は RFC 1094 として 1989年）は、**サーバがクライアントの状態を持たない**設計を採りました。サーバが再起動してもクライアントが壊れない、という当時としては画期的な堅牢性を得るためです。

その代償として、次のものが失われました。

| 失われたもの | 結果 |
|---|---|
| サーバ側の `open` 状態 | 「最後の FD が閉じたら削除」ができない → **silly rename の発明** |
| 「末尾に追記」という手続き | `O_APPEND` をクライアント側でエミュレートするしかない → **追記の競合** |
| 強い一貫性 | **close-to-open 一貫性**という妥協 |

NFSv3（1995年、RFC 1813）、NFSv4（2000年 RFC 3010 / 2003年 RFC 3530）、**NFSv4.1（2010年、RFC 5661）** と進化し、v4 でサーバ側に状態（OPEN、ロック、デリゲーション）が戻ってきました。しかし **「アトミックな追記」は今も存在しません。**

### 12.4 2001年 / 2014年：Log4j 1.x と Log4j 2

- **Log4j 1.x** の `DailyRollingFileAppender` は、同期の不備やロールオーバの信頼性の低さで有名でした。javadoc 自体に「同期の問題があり、データ損失の可能性がある」旨が書かれ、最終的に非推奨になりました。
- **Log4j 2.0**（2014年7月 GA）は、`RollingFileAppender` を「トリガポリシー（いつ回すか）」と「ロールオーバ戦略（どう回すか）」に分離し、プロセス**内**のスレッド安全性を大きく改善しました。
- しかし **プロセス間（複数 JVM）の安全性は、当時も今も提供されていません。** Log4j2 の FAQ/ドキュメントは「複数の JVM から同じファイルに書きたい場合は、ファイル共有ではなく `SocketAppender` などで集約せよ」という趣旨を示しています（正確な文言・掲載箇所はバージョンにより異なる／要確認）。
- **2021年12月：CVE-2021-44228（Log4Shell）**。この緊急対応で多くの現場が 2.17 系以降へ一斉に上げました。このとき**ログ設定を大きく見直した現場と、バージョンだけ上げた現場**に分かれ、後者では 2014年当時の設計（EFS 共有ファイルへの書き込みなど）がそのまま残りました。今回のような構成は、その系譜にあることが多いです。

> **バージョンについての注記（要確認）**：本資料の内部挙動の説明は Log4j 2.x 系を前提としています。Log4j 3.x ではモジュール構成・パッケージ配置が再編されていますが、**「FD が rename に気づかない」「NFS の追記は原子的でない」という本質は OS 側の性質なのでバージョンに依存しません。** 実環境のバージョンでの `RollingFileManager` の細部挙動（特に rename 失敗時の扱い）は実機確認を推奨します。

### 12.5 2011年：12-Factor App が「ファイルに書くな」と言った

Heroku の Adam Wiggins らがまとめた **The Twelve-Factor App** の第XI条は「**Logs: ログをイベントストリームとして扱う**」です。

> A twelve-factor app never concerns itself with routing or storage of its output stream. It should not attempt to write to or manage logfiles. Instead, each running process writes its event stream, unbuffered, to `stdout`.
>
> **訳：** 12-Factor App は、自分の出力ストリームの経路や保存先に一切関知しない。ログファイルに書き込んだり管理したりしようとしてはならない。代わりに、実行中の各プロセスは自身のイベントストリームをバッファリングせずに `stdout` へ書き出す。

**この原則は、まさに今回のような問題を避けるために書かれました。** 「プロセスが複数あり、いつ増えていつ消えるか分からない環境では、ファイルの所有者を1つに決められない」という現実への回答です。

### 12.6 2014〜2017年：コンテナと EFS が問題を再発見させた

- **2014年：Docker 1.0**、**2015年：Amazon ECS GA**、**2017年：AWS Fargate**
- **2016年6月：Amazon EFS GA**（NFSv4.1 のマネージドサービス）

EFS の登場で、「コンテナから POSIX 共有ファイルシステムをマウントする」ことが**数クリックで**できるようになりました。その結果、**1985年から知られていた NFS の落とし穴を、2016年以降の何千というチームが再発見する**ことになります。

そして **ECS のローリングアップデート（`maximumPercent=200`）** が、最後のピースを埋めました。

> ゼロダウンタイムデプロイの代償は、**「旧タスクと新タスクが必ず同時に走る時間帯が存在する」**ことです。
> 「1つのファイルには1つの書き手」という暗黙の前提は、この瞬間に**構造的に破綻**します。

今回のご質問の状況は、**「50年前の UNIX の設計」「40年前の NFS の妥協」「30年前のログローテーションの作法」「10年前のコンテナのデプロイ戦略」が、1つの `trace.log` の上で衝突している**、という構図です。

---

## 13. 用語集（小学生向けのたとえつき）

| 用語 | 正確な意味 | たとえ |
|---|---|---|
| **inode（アイノード）** | ファイルの実体。中身・サイズ・更新時刻を持つ。**名前は持たない** | ノートそのもの（紙の束） |
| **ファイル名 / ディレクトリエントリ** | 「この名前は inode 番号◯◯です」という対応表の1行 | ノートに貼った名前シール |
| **FD（ファイルディスクリプタ）** | プロセスが「このファイルを使っています」と示す整数。**inode に結びつく** | 「このノート使ってます」の整理券 |
| **`open()`** | 名前から inode を探して、FD を作る | 名前シールを見てノートを探し、整理券をもらう |
| **`rename()`** | 名前シールを貼り替える。**inode は動かない** | シールの貼り替え。ノートは同じ |
| **`unlink()`（削除）** | 名前シールを剥がす。誰も整理券を持っていなければノートも捨てる | シールを剥がす。使っている人がいれば捨てない |
| **`O_APPEND`** | 「いつも末尾に書く」というお願い。**ローカルでは確実、NFS では不確実** | 「必ず最後のページに書いてね」というお願い |
| **silly rename（`.nfs*`）** | NFS で「使用中のファイルを削除」しようとしたとき、隠しファイルに改名して延命する仕組み | 捨てようとしたノートを、使い終わるまで倉庫の隅に隠しておく |
| **close-to-open 一貫性** | 閉じてから開き直すまで、他人の書き込みは見えない | ノートを閉じて開き直さないと、他の人が書いた分が見えない |
| **ロールオーバ** | ログファイルを区切って新しいファイルに切り替えること | ノートを1冊書き終えて次の冊に移ること |
| **`TimeBasedTriggeringPolicy`** | 時刻でロールオーバを起こすポリシー。**周期は `%d` の最小単位で決まる** | 「毎時0分になったらノートを替えよう」という約束 |
| **DirectWrite** | `fileName` を書かず、`filePattern` のファイルに直接書く方式。**rename が起きない** | 最初から「23時のノート」に直接書く。貼り替え不要 |
| **ローリングアップデート** | 旧タスクを残したまま新タスクを立ち上げ、順次入れ替える方式 | 当番の交代。しばらく2人が同時にいる時間がある |
| **ESTALE** | NFS で「参照していたファイルがサーバ側でもう無い」エラー | 整理券を出したのに「そのノートはもうありません」と言われる |

---

## 14. チェックリスト

### 14.1 今すぐ確認すべきこと

- [ ] 本番の `/mnt/efs/logs/` に `.nfs*` ファイルが無いか
- [ ] 回転済みファイル（`trace-*.log`）の**最終行のタイムスタンプ**が、そのファイル名の時間帯を超えていないか
- [ ] 極端にサイズが小さい（ほぼ空の）回転済みファイルが無いか
- [ ] 稼働中 JVM の FD が `trace.log` 以外を指していないか（`ls -l /proc/<pid>/fd`）
- [ ] ログに NUL バイトや行の途中割り込みが混ざっていないか
- [ ] Log4j2 の `StatusLogger` 出力（stderr）に rename 失敗が出ていないか
- [ ] デプロイ用パイプラインの**実際のタスク起動時刻**（00:00 ちょうどか、前後にずれているか）

### 14.2 対策実施のステップ

- [ ] **STEP 0（当日）**：デプロイ時刻を 00:00 → 00:20 へ移動（応急処置）
- [ ] **STEP 1**：`interval="1"` を明示し、周期が `%d` の最小単位で決まる旨のコメントを設定ファイルに追記
- [ ] **STEP 2**：監視（第11章の項目1〜4）を追加し、現状の事故発生率を実測
- [ ] **STEP 3**：本対策の方式を決定（案1 stdout ／ 案2 DirectWrite+タスクID別パス）
- [ ] **STEP 4**：ログ収集側（EFS 直読みバッチ、Fluentd の glob など）の改修
- [ ] **STEP 5**：ステージング環境で、ローリングアップデートを**時境界をまたぐ時刻**に意図的に実行して検証
- [ ] **STEP 6**：本番適用。適用後1週間は STEP 2 の監視で回帰を確認
- [ ] **STEP 7**：応急処置（STEP 0）を元に戻すか判断

---

## 15. 参考情報と要確認事項

### 15.1 一次情報として当たるべきもの

| 対象 | 確認先 |
|---|---|
| `O_APPEND` と NFS の競合 | `man 2 open` の NOTES セクション |
| FD と inode の関係 | `man 2 open`, `man 7 inode`, `man 2 rename`, `man 2 unlink` |
| silly rename | Linux カーネル NFS クライアントのドキュメント、`nfs(5)` |
| NFSv4.1 仕様 | RFC 5661 |
| Log4j2 `RollingFileAppender` | Apache Log4j 2 公式マニュアル「Appenders」 |
| Log4j2 の複数JVM共有に関する見解 | Apache Log4j 2 公式 FAQ |
| ECS デプロイ設定 | AWS 公式「Amazon ECS デプロイタイプ / ローリング更新」 |
| ECS タスクメタデータ v4 | AWS 公式「タスクメタデータエンドポイントバージョン 4」 |
| EFS のロック・性能特性 | AWS 公式「Amazon EFS の制限」「パフォーマンス」 |
| 12-Factor App | https://12factor.net/logs |

### 15.2 本資料で「要確認」としている箇所

1. **Log4j2 の rename 失敗時の正確な挙動**（`RollingFileManager` がストリームを開き直すか、旧ストリームを保持するか）はバージョンにより差があります。**実環境のバージョンのソースまたは実機で確認してください。**
2. **別クライアントから rename 上書きされた inode への書き込みが `ESTALE` になるか**は、NFSv4 のサーバ実装依存です。**EFS での実挙動は実機検証を推奨します。**
3. **EFS のメタデータ操作レイテンシ**（本資料では 1ms 前後と仮定）は、スループットモード・リージョン・負荷により変動します。シナリオ C の確率計算はこの仮定に依存します。
4. **Log4j2 のバージョン**。本資料は 2.x 系を前提としています。3.x 系を使用している場合、設定要素名やモジュール構成が異なる可能性があります。
5. **`${env:VAR:-default}` のデフォルト値構文**のサポート状況はバージョンにより異なる場合があります。
6. **ECS の `HOSTNAME` 環境変数**の値（コンテナID か否か）は起動タイプにより異なるため、タスクメタデータエンドポイント経由での取得を推奨しています。

### 15.3 本資料の確率計算で用いた仮定

| 仮定 | 値 | 根拠 |
|---|---|---|
| ログ書き込み間隔 `W` | 30 秒 | ご提示の前提 |
| 回転遅延 `Δ` の分布 | `U(0, W)` | 書き込みタイミングと時境界の位相が無相関と仮定 |
| 新タスクの open 時刻 `D` | シナリオごとに `U(a,b)` | ECS の起動所要時間の実測が無いため範囲で提示 |
| rename〜create の隙間 `G` | 2 ミリ秒 | EFS メタデータ往復 約1ms × 2操作 と仮定 |
| 旧タスクが新タスクより後にロールする確率 | 0.5 | 両者の書き込み位相が独立と仮定 |

**これらは実測値ではなくモデルです。** 添付の Excel ファイル（`05_発生確率モデル` シート）では上記パラメータを入力セルとして編集できるようにしてあります。実環境の起動時間を計測して差し替えてください。

---

## 付録A：「なぜ書き込み間隔を短くすると安全になるのか」の直感的説明

ロールオーバは**時計ではなく、次のログ出力**で起きます。

```
書き込み間隔 30秒の場合:

 23:59:45 ●書込                                     ← このとき trace.log は inode #1
 00:00:00 ┊ ← 時境界（でも誰も何もしない）
          ┊  ← ★危険な窓（30秒間ずっと開いている）★
 00:00:15 ●書込 → ここで初めて回転する

 この「危険な窓」の間に新タスクが open すると、
 新タスクは回転前の inode #1 を掴んでしまう。


書き込み間隔 1秒の場合:

 23:59:59 ●書込
 00:00:00 ┊ ← 時境界
          ┊ ← 危険な窓は1秒しかない
 00:00:00 ●書込 → すぐ回転

 窓が 1/30 に縮むので、事故確率も大きく下がる。
```

ただしこれは**確率を下げるだけ**で、根治ではありません。また I/O 回数が増えるため EFS のコストとレイテンシに影響します。**本対策（案1または案2）を優先してください。**

---

## 付録B：判断フローチャート

```
                  ログを EFS に置く必要が本当にありますか？
                              │
              ┌───────────────┴───────────────┐
             いいえ                          はい
              │                               │
              ▼                               ▼
      【案1】stdout + FireLens        EFS 上のファイルを
       または awslogs                 「他のプロセス」も読みますか？
       ★最推奨・根治                            │
                              ┌────────────────┴────────────────┐
                            いいえ                             はい
                              │                                 │
                              ▼                                 ▼
                    【案5】エフェメラル         タスクIDでディレクトリを
                     ストレージ + サイドカー転送    分けても読み手は対応できますか？
                                                                │
                                                 ┌──────────────┴──────────────┐
                                               はい                          いいえ
                                                 │                             │
                                                 ▼                             ▼
                                    【案2】DirectWrite +          読み手側を改修する
                                     タスクID別パス                （改修できないなら案1へ）
                                     ★EFS 継続時の最推奨
```

---

*本資料は 2026-09-18 時点の理解に基づいて作成しました。「要確認」と記した箇所は、実環境のバージョンおよび実機での検証をお願いします。*
